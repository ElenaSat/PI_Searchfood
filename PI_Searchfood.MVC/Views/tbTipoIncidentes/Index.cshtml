@model IEnumerable<PI_Searchfood.MVC.DAL.tbTipoIncidente>

@{
    ViewBag.Title = "Index";
}

<h2>Tipo Incidente</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_tipo }) |
            @Html.ActionLink("Ver", "Details", new { id=item.Id_tipo }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Id_tipo })
        </td>
    </tr>
}

</table>
